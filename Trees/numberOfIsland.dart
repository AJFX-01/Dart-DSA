class NumberOfIsland {
  int numsIslands(List<List<int>> grid) {
    if (grid.isEmpty || grid[0].isEmpty) return 0;

    int numRows = grid.length;
    int numCols = grid[0].length;

    
  }

  void dfs() {}
}
